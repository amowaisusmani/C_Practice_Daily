question - Write a c program to replace 0 and 1 in a number

explanation - we take input of a binary string in an array format, and then traverse the whole array and look at it index-by-index. if the number encountered is a 0 we change it to 1 and vice-a-versa.